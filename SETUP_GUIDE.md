# 🔧 Walk & Point 웹 사이트 Firebase 완전 초보자용 가이드

이 가이드는 프로그래밍이나 서버 경험이 전혀 없는 분들도 쉽게 따라할 수 있도록 아주 상세하게 작성되었습니다. 화면에 보이는 대로 천천히 하나씩 따라오시면 됩니다!

---

## 1단계 : Firebase 가입 및 프로젝트 만들기

Firebase(파이어베이스)는 우리가 만든 웹사이트의 **"데이터베이스(점수 저장)"**와 **"서버(웹사이트 띄우기)"** 역할을 해줄 구글의 무료 서비스입니다.

1. 인터넷 창을 열고 구글 계정으로 로그인한 상태에서 [Firebase 콘솔(여기를 클릭)](https://console.firebase.google.com/)에 접속합니다.
2. 화면 중앙에 있는 하얀색 **[+ 프로젝트 추가]** 버튼을 클릭합니다.
3. 프로젝트 이름 첫 번째 입력칸에 `walk-win` 이라고 적고 파란색 **[계속]** 버튼을 누릅니다.
4. 구글 애널리틱스(방문자 통계) 설정 화면이 나오면, 아래쪽의 파란색 스위치를 눌러서 **[사용 안함]** 으로 회색으로 끄고 **[프로젝트 만들기]** 버튼을 누릅니다. (선택사항이므로 끄는 것이 편합니다.)
5. 빙글빙글 로딩이 1분 정도 지나고 "새 프로젝트가 준비되었습니다"라고 나오면 **[계속]**을 누릅니다.

---

## 2단계 : 웹사이트(앱) 등록하고 연결 코드 복사하기

우리가 만든 컴퓨터 안의 코드를 방금 만든 구글 Firebase 서버와 연결하는 과정입니다.

1. 프로젝트 메인 화면으로 들어오면, 중앙에 동그란 아이콘들이 보입니다. (iOS, Android, <b>`</>`</b>, Unity 등)
2. 여기서 세 번째에 있는 하얀색 **`</>` (웹)** 아이콘을 클릭합니다.
3. 앱 닉네임 입력칸에 `walk-win-web` 이라고 적고 파란색 **[앱 등록]** 버튼을 누릅니다.
4. "Firebase SDK 추가" 화면이 나오면 밑에 검은색 코드 상자가 보입니다.
5. 검은색 코드 상자 안에서 아래와 같이 `const firebaseConfig = { ... };` 라고 되어있는 부분(중괄호 안의 내용 전체)을 찾아 **복사(Ctrl+C)** 합니다.
   *(※ npm 사용 체크박스가 켜져 있다면, 옆에 있는 **<script> 태그 사용**을 누르면 잘 보입니다)*
6. 밑에 있는 파란색 **[콘솔로 이동]** 버튼이 나올 때까지 쭉 계속 누르고 나옵니다.
7. 이제 우리 코드 파일 중에서 `js/firebase-config.js` 파일을 엽니다. (메모장으로 열어도 됩니다.)
8. 파일 안쪽 위쪽에 `const firebaseConfig = { ... }` 라고 적혀있는 임시 내용을, 방금 구글에서 **[복사한 내용]으로 통째로 덮어쓰기(붙여넣기) 후 저장(Ctrl+S)** 합니다.

---

## 3단계 : 필수 기능 3가지 켜주기 (매우 중요!!!)

우리 웹사이트가 정상 작동하려면 다음 3가지 스위치를 꼭 켜주어야 합니다.

### 기능 1: 회원가입/로그인 관리 (Authentication)
1. Firebase 콘솔 화면의 **왼쪽 메뉴**에서 **[빌드]** 글자를 클릭해서 메뉴를 아래로 펼칩니다.
2. 메뉴 중 **[Authentication]**을 클릭하고, 화면 가운데 나타나는 **[시작하기]** 버튼을 누릅니다.
3. 상단의 **[Sign-in method]** 탭이 보이면, "로그인 제공업체 추가" 목록에서 **[이메일/비밀번호]**를 클릭합니다.
4. 첫 번째 스위치인 "이메일/비밀번호"를 클릭해서 **파란색으로 켜고 [저장]**을 누릅니다.

### 기능 2: 점수 및 QR 기록 저장 (Firestore Database)
1. **왼쪽 메뉴**에서 **[Firestore Database]**를 클릭하고, 화면 가운데 **[데이터베이스 만들기]**를 누릅니다.
2. 위치 설정이 나오면 영어로 `asia-northeast3 (Seoul)`을 선택하고 **[다음]**을 누릅니다. (서울이 가장 빠릅니다)
3. 보안 규칙 설정에서 **[테스트 모드에서 시작]**에 **동그라미 체크**를 하고 **[만들기]**를 누릅니다.

### 기능 3: 걷기 인증 스크린샷 이미지 저장 (Storage)
1. **왼쪽 메뉴**에서 **[Storage]**를 클릭하고, 화면 가운데 **[시작하기]**를 누릅니다.
2. 보안 규칙 설정에서 마찬가지로 **[테스트 모드에서 시작]**을 체크하고 **[다음]**을 누릅니다.
3. 위치는 아까 설정한 서울(`asia-northeast3`)이 기본으로 잡혀 있으니 그대로 **[완료]**를 누릅니다.

---

## 4단계 : 최고 관리자 계정 이메일 설정하기

웹사이트에서 우리 조원들이 남들이 올린 걷기 인증 샷을 '승인' 버튼을 눌러 점수를 주고, 참가자들의 총 점수를 조작할 수 있는 '슈퍼 계정'을 지정해야 합니다.

1. 우리 코드 폴더에서 `js/firebase-config.js` 파일을 한 번 더 엽니다.
2. 파일 중간쯤에 있는 아래 부분을 찾습니다.
   `const ADMIN_EMAIL = "admin@walk-challenge.com";`
3. 큰따옴표 안의 영어를 나중에 내가 웹사이트에 회원가입할 실제 내 이메일(예: `my-email@naver.com`)로 바꾼 뒤 **파일을 저장(Ctrl+S)** 합니다.

---

## 5단계 : 웹사이트 인터넷 배포하기 (가장 어려운 부분)

이 부분은 완성된 웹사이트를 전 세계 누구나 휴대폰으로 들어올 수 있게 인터넷(`https://`)에 올리는 과정입니다. 까만색 터미널 창(명령 프롬프트)을 만져야 합니다.

👉 **터미널이나 까만 창이 너무 복잡하고 어려우시다면, 직접 하지 마시고 저(AI)에게 채팅으로 이렇게 말씀해주세요!**
> "1~4단계까지 Firebase 설정 완료했어. 이제 네가 터미널로 5단계 배포 명령어 처리해줘!"
*(그럼 제가 알아서 창을 띄워 대신 명령어를 쳐서 인터넷 주소를 뽑아드립니다!)*

**[직접 해보시려면 아래 순서를 따르세요]**
1. 컴퓨터에 `Node.js` 가 안 깔려있다면 [nodejs.org](https://nodejs.org/)에서 다운받아 설치합니다.
2. 윈도우 검색창(시작버튼 옆)에 'cmd'를 입력해 **명령 프롬프트**를 엽니다.
3. 아래 코드를 복사해서 까만 창에 붙여넣고 엔터(Enter)를 칩니다.
   - `npm install -g firebase-tools` (수 분 정도 설치가 진행됩니다)
   - 전부 멈추면 이어서 `firebase login` 엔터
   - (Y/N)을 물어보면 키보드로 `Y` 누르고 엔터
   - 인터넷 구글 로그인 창이 뜨면 아까 프로젝트를 만든 내 구글 계정으로 로그인 후 허용을 누릅니다.
4. 이제 작업 중인 폴더로 검은 창의 이동 경로를 바꿉니다.
   - `cd "C:\2026 1학기 대학 과제 및 자료\스포츠비즈니스기획운영\walk-challenge"` 적고 엔터
5. 구글 서버와 초기 연결 명령어를 칩니다.
   - `firebase init hosting` 엔터
   - 질문 1: Please select an option: 키보드 아래 방향키로 **Use an existing project**로 내린후 엔터
   - 질문 2: 방금 1단계에서 만든 **walk-win 어쩌고** 프로젝트를 찾아 엔터
   - 질문 3: What do you want to use as your public directory? 키보드로 쩜 하나 **`.`** 만 적고 엔터
   - 질문 4: Configure as a single-page app? **`N`** 적고 엔터
   - 질문 5: Set up automatic builds and deploys with GitHub? **`N`** 적고 엔터
6. 마지막 업로드 명령어를 칩니다.
   - `firebase deploy` 엔터
   - 막대기가 쭈루룩 올라가고 완료됩니다.
7. 검은 창에 적혀나온 `Hosting URL: https://주소어쩌고.web.app`이 우리 챌린지의 진짜 웹 접속 주소입니다!

---

## 6단계 : QR코드 출력 및 진짜 준비 끝!

이제 스마트폰으로 그 주소에 들어갈 수 있습니다.

1. 배포된 실제 주소(`https://어쩌고.web.app`)로 접속합니다.
2. 우측 상단 **[참가 신청]** 버튼을 눌러, 아까 4단계에서 적었던 내 관리자 이메일 주소로 가입합니다.
3. 배포된 실제 주소 뒤에 `/admin.html` 을 붙여서 인터넷 주소창으로 직접 이동합니다. (예: `https://어쩌고.web.app/admin.html`)
4. 관리자 페이지가 뜹니다! 여기서 **[📱 QR 코드]** 글자 탭을 누릅니다.
5. 장소 10개가 쭉 뜨면 각각 **[⬇ 다운로드]** 버튼을 누릅니다.
6. 다운로드한 사진 크기를 적당히 조절해 A4 종이에 프린트한 후 캠퍼스 지정 장소에 하나씩 붙이면 됩니다.
7. 지나가던 학우들이 이 종이 QR코드를 휴대폰 카메라로 찍으면 자동으로 내 웹사이트 이벤트 화면으로 슝 넘어갑니다! 수고하셨습니다 👏

